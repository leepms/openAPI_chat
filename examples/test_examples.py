"""
ARCHIVED: test_examples.py

This testing helper validated many example files during development.
To keep the examples folder minimal we now rely on the project's
`tests/` suite (see `tests/README.md` and `test/test_all_features.py`).
"""

print("test_examples.py archived. Use the main tests in the tests/ folder.")
